apiVersion: v1
kind: Service
metadata:
  name: flux-schnell
  namespace: default
spec:
  selector:
    app: flux-schnell
  ports:
    - port: 8000
      targetPort: 8000
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flux-schnell
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flux-schnell
  template:
    metadata:
      labels:
        app: flux-schnell
    spec:
      nodeSelector:
        node-type: trn3
      resourceClaims:
        # s-lnc2-trn3 = 1 device = 4 NeuronCores x 24GB = 96GB. TP=4 across the cores.
        - name: s-lnc2-trn3
          resourceClaimTemplateName: s-lnc2-trn3
      containers:
        - name: app
          image: 621547421844.dkr.ecr.us-west-2.amazonaws.com/native-pytorch:2.11.0-neuronx-py312-sdk2.31.0-ubuntu24.04-neurondlcbuilder-development-6486001458-0
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - "-exc"
            - |
              set -euxo pipefail

              # Clone the FLUX.1-schnell project (serve.py + nki_upsample.py + requirements).
              git clone -b "${BRANCH:-master}" https://github.com/yahavb/FLUX.1-schnell.git repo
              cd repo

              # New DLC image has no auto-activated venv -> target system python3 via --python.
              UVPY="$(command -v python3)"
              echo "uv target interpreter: $UVPY"
              uv pip install --python "$UVPY" -r requirements.txt

              # HF login (new SDK: `hf`, not `huggingface-cli`) + local HF cache (/var/mdl is
              # S3/mountpoint-s3 which lacks os.replace -> hf_hub_download fails Errno 38).
              export HF_TOKEN
              hf auth login --token "$HF_TOKEN" || true
              export HF_HOME=/tmp/hf
              mkdir -p "$HF_HOME"

              # GATE: prove the NKI nearest-upsample kernel is correct (kernel on neuron vs
              # CPU reference, exact match) BEFORE launching the pipeline. If the kernel is
              # wrong, fail here — do NOT waste a full model load + compile on a broken VAE.
              echo "============================================"
              echo "  KERNEL GATE: test_nki_upsample.py (must PASS before serving)"
              echo "============================================"
              python3 test_nki_upsample.py | tee /tmp/kerneltest.log
              grep -q "ALL PASS" /tmp/kerneltest.log || { echo "NKI upsample kernel FAILED — not launching pipeline"; exit 1; }

              echo "============================================"
              echo "  FLUX.1-schnell on Trainium (torch.compile + TP=${TP_DEGREE})"
              echo "  ${WIDTH}x${HEIGHT}, ${NUM_STEPS} steps, guidance ${GUIDANCE_SCALE}"
              echo "============================================"

              # SINGLE CORE, plain python3 (no torchrun/TP). serve.py pins
              # NEURON_RT_VISIBLE_CORES=0, runs text encoders eager on host, compiles the
              # transformer FF submodules (attn eager — compiling flux attn NaNs), VAE fp32.
              python3 serve.py
          resources:
            claims:
              - name: s-lnc2-trn3
            # s-lnc2-trn3 = 1 device/pod -> 16 pods/node. Requests under node/16
            # (~11 cpu, ~122Gi mem, ~115Gi ephemeral) so 16 stack; limits burst.
            requests:
              cpu: 10
              memory: 110Gi
              ephemeral-storage: 60Gi
            limits:
              cpu: 22
              memory: 122Gi
              ephemeral-storage: 100Gi
          volumeMounts:
            - name: dshm
              mountPath: /dev/shm
            - name: 621547421844-ap-southeast-4-pvc
              mountPath: /var/mdl
          env:
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: NEURON_RT_LOG_LEVEL
              value: "ERROR"
            - name: NEURON_CC_LOG_LEVEL
              value: "ERROR"
            - name: TORCH_NEURONX_LOG_LEVEL
              value: "ERROR"
            - name: NEURON_CC_FLAGS
              value: "--model-type=transformer"
            - name: NEURON_RT_DBG_INTRA_RDH_CHANNEL_BUFFER_SIZE
              value: "134217728"
            # PROVEN serve config (bench run cl8rs): compile the transformer FF submodules,
            # keep attention EAGER (compiling flux attn -> NaN/black image on this SDK).
            # Correct image, ~4770ms/img at 640x480, single core.
            - name: FLUX_COMPILE_MODE
              value: "ff_only"
            - name: MODEL_NAME
              value: "black-forest-labs/FLUX.1-schnell"
            - name: HEIGHT
              value: "480"
            - name: WIDTH
              value: "640"
            - name: NUM_STEPS
              value: "4"
            - name: GUIDANCE_SCALE
              value: "0.0"
            - name: MAX_SEQ_LEN
              value: "256"
            - name: DTYPE
              value: "bfloat16"
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hf-token
                  key: HF_TOKEN
          ports:
            - containerPort: 8000
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health
              port: 8000
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 475        # first-call NEFF compile can take many minutes
          readinessProbe:
            httpGet:
              path: /readiness
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 600
            periodSeconds: 300
            failureThreshold: 1000
            timeoutSeconds: 60
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 32Gi
        - name: 621547421844-ap-southeast-4-pvc
          persistentVolumeClaim:
            claimName: 621547421844-ap-southeast-4-pvc
